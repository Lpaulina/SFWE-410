spring:
  application:
    name: eurekaserver
  profiles:
    active: ${SPRING_PROFILES_ACTIVE}
  cloud:
    config:
      uri: ${SPRING_CLOUD_CONFIG_URI:http://configserver:8071}
  config:
    import: "optional:configserver:${SPRING_CLOUD_CONFIG_URI:http://configserver:8071}"